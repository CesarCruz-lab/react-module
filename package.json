{
  "name": "react-module",
  "version": "1.0.0",
  "description": "test only",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cesar (https://github.com/CesarCruz-lab)",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
